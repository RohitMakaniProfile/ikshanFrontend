# Optional: only if you need to override API URL
# VITE_API_BASE_URL=http://35.200.180.98

# For Vercel deploy: set this in Vercel Dashboard → Project → Settings → Environment Variables
# so /api requests go through Vercel rewrites (no CORS issues)
# VITE_VERCEL_DEPLOY=true
